--[[ 
    PolarisHub | 2026 Advanced Edition 
    Author: Sebestianmek 
    Build: 3.2.0 (Security Patched - Keyless)
]]

-- [ CONFIGURATION ]
local WALKSPEED_OFFSET = 0x1D4 

-- [ SERVICES ]
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local HttpService = game:GetService("HttpService")
local Camera = workspace.CurrentCamera

-- [ UI LOAD ]
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- [ INITIALIZE UI WINDOW ]
local Window = Rayfield:CreateWindow({
    Name = "PolarisHub | 2026 Advanced Edition",
    LoadingTitle = "Initializing Polaris Systems...",
    LoadingSubtitle = "by Sebestianmek",
    ConfigurationSaving = { Enabled = true, FolderName = "PolarisConfig", FileName = "MainHub" },
    KeySystem = false, -- Key system disabled
    KeySettings = {
        Title = "Access Terminal",
        Subtitle = "Enter your Website Key",
        Note = "Keys are UserID locked and Time-Gated.",
        FileName = "PolarisKey",
        SaveKey = true,
        GrabKeyFromSite = false,
        Key = {"Hello"} -- Placeholder (Not used since KeySystem is false)
    }
})

-- Global State
local AimbotEnabled, ESPEnabled, TeamCheck, InfiniteJump = false, false, false, false
local WalkSpeedValue, GiveAmount = 16, 0

-- [ TABS ]
local MainTab = Window:CreateTab("Automation", 4483362458)
local CombatTab = Window:CreateTab("Combat", 4483362458)
local MovementTab = Window:CreateTab("Movement", 4483362458)
local VisualsTab = Window:CreateTab("Visuals", 4483362458)
local RemoteTab = Window:CreateTab("Network Spy", 4483362458)

-- [ AUTOMATION TAB ]
MainTab:CreateSection("Economy & Utilities")
MainTab:CreateButton({
    Name = "Open Developer Console (F9)",
    Callback = function() 
        game:GetService("VirtualInputManager"):SendKeyEvent(true, Enum.KeyCode.F9, false, game) 
    end,
})

MainTab:CreateInput({
    Name = "Amount to Inject",
    PlaceholderText = "E.g. 99999",
    Callback = function(Text) GiveAmount = tonumber(Text) or 0 end,
})

MainTab:CreateButton({
    Name = "Auto-Find Money Remotes",
    Callback = function()
        local count = 0
        for _, v in pairs(game:GetDescendants()) do
            if v:IsA("RemoteEvent") and (v.Name:lower():find("money") or v.Name:lower():find("add") or v.Name:lower():find("cash")) then
                v:FireServer(GiveAmount)
                count = count + 1
            end
        end
        Rayfield:Notify({Title = "Scan Complete", Content = "Fired " .. count .. " potential remotes.", Duration = 3})
    end,
})

-- [ COMBAT TAB ]
CombatTab:CreateSection("Aimbot Engine")
CombatTab:CreateToggle({
    Name = "Master Aimbot",
    CurrentValue = false,
    Flag = "AimbotToggle",
    Callback = function(Value) AimbotEnabled = Value end,
})

CombatTab:CreateToggle({
    Name = "Team Check",
    CurrentValue = false,
    Callback = function(Value) TeamCheck = Value end,
})

RunService.RenderStepped:Connect(function()
    if AimbotEnabled then
        local ClosestPlayer = nil
        local ShortestDistance = math.huge
        local MousePos = UserInputService:GetMouseLocation()

        for _, player in pairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
                local Hum = player.Character:FindFirstChild("Humanoid")
                if Hum and Hum.Health > 0 then
                    if TeamCheck and player.Team == LocalPlayer.Team then continue end
                    
                    local Pos, OnScreen = Camera:WorldToViewportPoint(player.Character.HumanoidRootPart.Position)
                    if OnScreen then
                        local Distance = (Vector2.new(Pos.X, Pos.Y) - MousePos).Magnitude
                        if Distance < ShortestDistance then
                            ClosestPlayer = player
                            ShortestDistance = Distance
                        end
                    end
                end
            end
        end

        if ClosestPlayer then
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, ClosestPlayer.Character.HumanoidRootPart.Position), 0.15)
        end
    end
end)

-- [ MOVEMENT TAB ]
MovementTab:CreateSection("Character Physics")
MovementTab:CreateSlider({
    Name = "WalkSpeed Modifier",
    Range = {16, 250},
    Increment = 1,
    CurrentValue = 16,
    Flag = "WalkSpeedSlider",
    Callback = function(Value) WalkSpeedValue = Value end,
})

MovementTab:CreateToggle({
    Name = "Infinite Jump",
    CurrentValue = false,
    Callback = function(Value) InfiniteJump = Value end,
})

RunService.Heartbeat:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = WalkSpeedValue
    end
end)

UserInputService.JumpRequest:Connect(function()
    if InfiniteJump and LocalPlayer.Character and LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid") then
        LocalPlayer.Character:FindFirstChildWhichIsA("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)

-- [ VISUALS TAB ]
VisualsTab:CreateSection("World Visuals")
VisualsTab:CreateToggle({
    Name = "Player Highlights (ESP)",
    CurrentValue = false,
    Callback = function(Value)
        ESPEnabled = Value
        if not Value then
            for _, p in pairs(Players:GetPlayers()) do
                if p.Character and p.Character:FindFirstChild("PolarisESP") then
                    p.Character.PolarisESP:Destroy()
                end
            end
        end
    end,
})

task.spawn(function()
    while task.wait(1) do
        if ESPEnabled then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and not p.Character:FindFirstChild("PolarisESP") then
                    local Highlight = Instance.new("Highlight")
                    Highlight.Name = "PolarisESP"
                    Highlight.Parent = p.Character
                    Highlight.FillColor = Color3.fromRGB(0, 120, 255)
                    Highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
                    Highlight.FillTransparency = 0.5
                end
            end
        end
    end
end)

-- [ NETWORK SPY TAB ]
local SpyEnabled = false
RemoteTab:CreateSection("Traffic Logger")
RemoteTab:CreateToggle({
    Name = "Enable Network Spy",
    CurrentValue = false,
    Callback = function(Value) SpyEnabled = Value end,
})

local old; old = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    local args = {...}
    if SpyEnabled and (method == "FireServer" or method == "InvokeServer") then
        print("[POLARIS SPY] Target: " .. self.Name .. " | Method: " .. method)
        for i, v in pairs(args) do
            print("  Arg["..i.."]: " .. tostring(v))
        end
    end
    return old(self, ...)
end)

Rayfield:LoadConfiguration()
Rayfield:Notify({Title = "Status", Content = "Polaris Systems Fully Operational (Keyless)", Duration = 3})
